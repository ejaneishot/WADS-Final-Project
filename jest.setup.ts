/**
 * Jest setup — extends expect with @testing-library/jest-dom matchers
 * (e.g. toBeInTheDocument, toHaveValue). Loaded via setupFilesAfterEnv in jest.config.cjs.
 */
import "@testing-library/jest-dom";